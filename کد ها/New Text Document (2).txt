    <!-- بخش دوم و سوم: ساختار HTML و منطق JavaScript -->
    <div class="container">
        
        <!-- Hero Section -->
        <section class="hero fade-in">
            <div class="icon-wrapper">
                <!-- یک آیکون اختصاصی و شناور -->
                <svg width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="#5e6ad2" stroke-width="1" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path></svg>
            </div>
            <h1>Mohammad Shekari</h1>
            <p>Full-Stack Developer & System Architect</p>
        </section>

        <!-- Main Content Area -->
        <main class="nested-container fade-in">
            
            <!-- Tech Stack Section -->
            <section id="skills">
                <h2 style="margin-bottom: 30px; font-size: 1.5rem;">Technical Arsenal</h2>
                <div class="tech-grid">
                    <div class="tech-item">
                        <svg viewBox="0 0 24 24"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path></svg>
                        <span>Python</span>
                    </div>
                    <div class="tech-item">
                        <svg viewBox="0 0 24 24"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path></svg>
                        <span>Flask</span>
                    </div>
                    <div class="tech-item">
                        <svg viewBox="0 0 24 24"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path></svg>
                        <span>Docker</span>
                    </div>
                    <div class="tech-item">
                        <svg viewBox="0 0 24 24"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path></svg>
                        <span>ESP32</span>
                    </div>
                </div>
            </section>

            <!-- Code Block Section (Red-Box/Nested Style) -->
            <section id="code-display" style="margin-top: 60px;">
                <div class="red-box">
                    <h3 style="margin-bottom: 15px; color: var(--accent-blue);">System Architecture Implementation</h3>
                    <div class="code-section">
                        <div class="code-header">
                            <div class="dot red"></div>
                            <div class="dot yellow"></div>
                            <div class="dot green"></div>
                        </div>
<pre><code>def initialize_system(config):
    # Linear-style architecture setup
    status = "ACTIVE"
    for module in config.modules:
        print(f"Initializing {module}...")
        connect_to_hardware(module)
    return status</code></pre>
                    </div>
                </div>
            </section>

            <!-- Grafana-style Chart Section -->
            <section id="analytics" style="margin-top: 60px;">
                <h3 style="margin-bottom: 20px;">System Performance (Grafana Style)</h3>
                <div class="grafana-card">
                    <div class="chart-area" id="grafana-chart">
                        <!-- Bars will be generated by JS -->
                    </div>
                </div>
            </section>

        </main>

        <footer style="text-align: center; padding: 50px 0; color: var(--text-secondary); font-size: 0.8rem;">
            &copy; 2026 Mohammad Shekari. Built with precision.
        </footer>
    </div>

    <script>
        // 1. Fade-in Animation on Scroll
        const observerOptions = { threshold: 0.1 };
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));

        // 2. Grafana Chart Generator (Dynamic Bars)
        const chartContainer = document.getElementById('grafana-chart');
        const dataPoints = [40, 70, 45, 90, 65, 85, 30, 55, 95, 75, 60, 80];

        function generateChart() {
            chartContainer.innerHTML = ''; // Clear
            dataPoints.forEach(val => {
                const bar = document.createElement('div');
                bar.className = 'grafana-bar';
                bar.style.height = '0%'; // Start at 0 for animation
                chartContainer.appendChild(bar);
                
                // Trigger animation after a tiny delay
                setTimeout(() => {
                    bar.style.height = val + '%';
                }, 100);
            });
        }

        // Initialize everything
        window.addEventListener('DOMContentLoaded', () => {
            generateChart();
        });
    </script>
</body>
</html>
